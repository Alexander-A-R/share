import React from 'react';
import ReactDOM from 'react-dom';
import Share from './components/ShareButton/ShareButton';


ReactDOM.render(<Share style={{position: 'relative', left: '10%'}} />, document.getElementById('root')
);
